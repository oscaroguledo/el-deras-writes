feat: Add Django management command to update user titles

- Created a new Django management command `update_user_titles` in `backend/blog/management/commands/update_user_titles.py`.
- This command iterates through existing `CustomUser` instances and assigns a random title from `TITLE_CHOICES` if their `title` field is empty.