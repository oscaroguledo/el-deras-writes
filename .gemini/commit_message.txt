fix: Correctly render category name in ArticleDetail

Modified `frontend/src/pages/ArticleDetail.tsx` to correctly render `article.category.name` and use it in the `Link` component's `to` prop. This resolves the "Objects are not valid as a React child" error when displaying category information.